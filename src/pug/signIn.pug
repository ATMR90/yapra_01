extends layout/main

block scripts
	link(rel="stylesheet", href="/src/css/signIn.css")
	script(src="signIn.js") 

block content 
	.ya-sign-in
		.card.ya-sign-in__card.card_shadow
			.card__title Вход
			.card__form
				.card__fields
					+input('login','text','Логин')
					+input('password','password','Пароль')
				.submit.card__submit 
					.submit__error#submit__error 
					.submit__btn(onclick="signInBtn()") Войти
			.card__footer 
				| Нет аккаунта? 
				a.card_link(href="signUp.pug") Регистрация
