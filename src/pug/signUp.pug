extends layout/main

block scripts
	link(rel="stylesheet", href="/src/css/signUp.css")
	script(src="signUp.js") 

block content
	.ya-sign-up
		.card.ya-sign-up__card.card_shadow
			.card__title Регистрация
			.card__form
				.card__fields
					+input('email','text','Почта')
					+input('first_name','text','Имя')
					+input('second_name','text','Фамилия')
					+input('phone','text','Телефон')
					.input
						input.input__field.input__field_text#login(type="text" oninput="loginInputHandler()")
						.input__label Логин
					.input 
						input.input__field.input__field_text#passwordOne(type="password" oninput="passwordOneInputHandler()")
						.input__label Пароль
					+input('passwordTwo','password','Пароль еще раз')
				.submit.card__submit 
					.submit__error#submit__error 
					.submit__btn(onclick="singUpBtn()") Регистрация
			.card__footer 
				| Есть аккаунт? 
				a.card_link(href="signIn.pug") Вход